{
  "name": "ot-rich-text",
  "version": "0.1.1",
  "description": "Format for representing rich-text documents and changes.",
  "main": "index.js",
  "scripts": {
    "start": "nodemon",
    "test": "tap test/**/*.js",
    "tdd": "nodemon ./node_modules/.bin/tap test/*.js",
    "test-coverage": "npm test -- --cov --coverage-report=lcov",
    "tdd-coverage": "nodemon ./node_modules/.bin/tap --cov --coverage-report=lcov --no-browser test/*.js",
    "tap": "tap",
    "tap-watch": "nodemon ./node_modules/.bin/tap",
    "prepublish": "cd node_modules/ot-fuzzer && npm i"
  },
  "author": "",
  "license": "UNLICENSED",
  "private": true,
  "repository": {
    "type": "git",
    "url": "git://github.com/Teamwork/ot-rich-text.git"
  },
  "devDependencies": {
    "nodemon": "^1.12.0",
    "ot-fuzzer": "Teamwork/fuzzer#diffX",
    "tap": "^10.7.2"
  },
  "dependencies": {
    "fast-diff": "^1.1.2"
  }
}
